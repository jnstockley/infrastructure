---
name: infisical
services:
  backend:
    image: infisical/infisical:v0.160.12
    container_name: infisical-backend
    environment:
      - NODE_ENV=production
      - REDIS_URL=redis://redis:6379
    env_file: .env
    networks:
      - proxy
      - private
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.http.routers.infisical.entrypoints=websecure
      - traefik.http.routers.infisical.rule=Host(`secrets.jstockley.com`)
      - traefik.http.routers.infisical.tls=true
      - traefik.http.routers.infisical.tls.certresolver=production
      - traefik.http.routers.infisical.service=infisical
      - traefik.http.services.infisical.loadbalancer.server.port=8080
      - traefik.docker.network=proxy
    pull_policy: always

networks:
  proxy:
    name: proxy
    external: true
    enable_ipv4: true
    enable_ipv6: true
  private:
    name: private
    internal: true
    external: true
    enable_ipv4: true
    enable_ipv6: true
