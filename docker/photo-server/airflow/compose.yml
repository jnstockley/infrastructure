services:
  airflow-webserver:
    image: jnstockley/airflow:62eefa7a590e36fe0fde78320df33ae0ce837164
    volumes:
      - ./logs:/opt/airflow/logs
      - ./data:/opt/airflow/data
    env_file:
      .env
    networks:
      - airflow
      - postgres
      - twingate
    command: webserver
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/health"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 100s
    restart: unless-stopped
    container_name: airflow-webserver
    depends_on:
      - airflow-scheduler
    labels:
      - traefik.enable=true
      - traefik.http.routers.airflow.entrypoints=websecure
      - traefik.http.routers.airflow.rule=Host(`airflow.local.jstockley.com`)
      - traefik.http.routers.airflow.tls=true
      - traefik.http.routers.airflow.tls.certresolver=production
      - traefik.http.routers.airflow.service=airflow
      - traefik.http.services.airflow.loadbalancer.server.port=8080
      - traefik.docker.network=airflow

  airflow-scheduler:
    image: jnstockley/airflow:62eefa7a590e36fe0fde78320df33ae0ce837164
    volumes:
      - ./logs:/opt/airflow/logs
      - ./data:/opt/airflow/data
    env_file:
      .env
    networks:
      - airflow
      - postgres
      - twingate
    command: scheduler
    healthcheck:
      test: [ "CMD", "curl", "--fail", "http://localhost:8974/health" ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 100s
    restart: always
    container_name: airflow-scheduler
    depends_on:
      - airflow-init

  airflow-init:
    image: jnstockley/airflow:62eefa7a590e36fe0fde78320df33ae0ce837164
    volumes:
      - ./logs:/opt/airflow/logs
      - ./data:/opt/airflow/data
    env_file:
      .env
    networks:
      - airflow
      - postgres
      - twingate
    command: version
    environment:
      _AIRFLOW_DB_MIGRATE: 'true'

networks:
  airflow:
    name: airflow
    external: true
  postgres:
    name: postgres
    external: true
  twingate:
    name: twingate
    external: true