---
name: bsn
services:
  bsn:
    image: jnstockley/bsn:v3.2.14
    container_name: bsn
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - LOG_LEVEL=DEBUG
      - ALLOW_SHORTS=False
      - TZ=America/Chicago
    env_file:
      - .env
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
