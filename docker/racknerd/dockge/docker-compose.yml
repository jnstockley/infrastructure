version: "3.8"

services:
  dockge:
    image: "louislam/dockge:1.4.1"
    restart: "unless-stopped"
    volumes:
      - "./data:/app/data"
      - "$XDG_RUNTIME_DIR/docker.sock:/var/run/docker.sock"
      - "/home/jackstockley/docker:/home/jackstockley/docker"
    environment:
      - "DOCKGE_STACKS_DIR=/home/jackstockley/docker"
    networks:
       - "dockge"

networks:
  dockge:
    name: "dockge"
    external: true